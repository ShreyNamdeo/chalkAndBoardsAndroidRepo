<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <item android:id="@android:id/background">
        <shape>
            <corners android:radius="5dip" />
            <gradient
                android:startColor="@color/light_gray"
                android:centerColor="@color/light_gray"
                android:centerY="0.75"
                android:endColor="@color/light_gray"
                android:angle="270"
                />
        </shape>
    </item>

    <item android:id="@android:id/secondaryProgress">
        <clip>
            <shape>
                <corners android:radius="5dip" />
                <gradient
                    android:startColor="@color/header_content_bg"
                    android:centerColor="@color/header_content_bg"
                    android:centerY="0.75"
                    android:endColor="@color/header_content_bg"
                    android:angle="270"
                    />
            </shape>
        </clip>
    </item>

    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <corners
                    android:radius="5dip" />
                <gradient
                    android:startColor="@color/appGreenColor"
                    android:endColor="@color/appGreenColor"
                    android:angle="270"
                    />
            </shape>
        </clip>
    </item>

</layer-list>